<?php

namespace App\Enums;

enum CurrencyEnum
{
    // Enums are used here as strings not as an integer. Its not ideal, but I try to let it more verbose for this test
    // case EUR;
    // case DOL;

    const EUR = 'EUR';
    const DOL = 'DOL';
}